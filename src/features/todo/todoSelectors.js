export const selectTodos = (state) => state.todos.todos;
export const selectFilteredTodos = (state) => state.todos.filtered;
export const selectTodosFromServer = (state) => state.todos.todosFromServer;
